{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "test -n \"${CLAUDE_PLUGIN_ROOT}\" && bash \"${CLAUDE_PLUGIN_ROOT}/scripts/fetch-usage.sh\" > /dev/null 2>&1 &"
          }
        ]
      }
    ]
  }
}
